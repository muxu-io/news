---
config: security-bulletins
date: '2026-04-12'
generated_at: '2026-04-12T08:53:02.796102+00:00'
items_processed: 114
sources_failed: 0
sources_fetched: 3
time_window: 3d
title: Security Bulletins
---

Okay, here's the security bulletin digest based on the provided content, following all the given rules and guidelines:

## Executive Summary

This digest highlights multiple critical vulnerabilities, primarily affecting the Linux Kernel, Rembg and PraisonAI. The Linux Kernel issues involve potential crashes and memory corruption, requiring immediate patching.  PraisonAI continues to be a major source of vulnerabilities, specifically related to command injection and missing permission checks.  Rembg is vulnerable to arbitrary file read via crafted requests.

## Critical / Actively Exploited

*   **Daptin: Unauthenticated Path Traversal and Zip Slip (CVSS: 10.0)** The `cloudstore.file.upload` action is vulnerable to path traversal and zip slip attacks, allowing unauthenticated attackers to write arbitrary files. *Upgrade to a patched version once released. Restrict access to the cloudstore.file.upload action through authentication and authorization controls until a patch is available.* GHSA-9cp7-j3f8-p5jx
*  **paperclip Vulnerable to Unauthenticated Remote Code Execution via Import Authorization Bypass** An unauthenticated attacker can achieve full remote code execution on any network-accessible Paperclip instance running in `authenticated` mode with default configuration. No user interaction, no credentials, just the target's address. *Upgrade or configure the Auth settings.* GHSA-68qg-g8mg-6pr7
*  **mathjs Allows Improperly Controlled Modification of Dynamically-Determined Object Attributes** Arbitrary JavaScript execution via the mathjs expression parser. You can be affected when you have an application where users can evaluate arbitrary expressions using the mathjs expression parser. *Upgrade to mathjs v15.2.0.* GHSA-jvff-x2qm-6286
*   **Axios has Unrestricted Cloud Metadata Exfiltration via Header Injection Chain (CVSS: 9.9)** A chain of vulnerabilities allows Prototype Pollution to escalate into Remote Code Execution or Cloud Compromise due to lack of HTTP Header Sanitization. *Update axios.* GHSA-fvcv-3m26-pcqx

## Kernel Security

*   **CVE-2026-31413: bpf: Fix unsound scalar forking in maybe_fork_scalars() for BPF_OR** A verifier/runtime divergence allows out-of-bounds map access. *Fixed in versions 6.12.80, 6.18.21, 6.19.11, and 7.0-rc5.*
*   **CVE-2026-31412: usb: gadget: f_mass_storage: Fix potential integer overflow in check_command_size_in_blocks()** Integer overflow can lead to memory corruption or out-of-bounds access. *Fixed in versions 6.1.167, 6.6.130, 6.12.78, and 6.18.19.*
*   **CVE-2026-31411: net: atm: fix crash due to unvalidated vcc pointer in sigd_send()** Userspace-controlled vcc pointer can cause a kernel dereference. *Fixed in version 5.10.252.*
*   **CVE-2026-31410: ksmbd: use volume UUID in FS_OBJECT_ID_INFORMATION** Use sb->s_uuid for a proper volume identifier as the primary choice. *Fixed in versions 6.12.78, 6.18.20, 6.19.10, and 7.0-rc5.*
*   **CVE-2026-31409: ksmbd: unset conn->binding on failed binding request** The connection remains in a binding state where all subsequent ksmbd_session_lookup_all() calls fall back to the global sessions table. *Fixed in versions 6.1.167, 6.6.130, 6.12.78, 6.18.20, 6.19.10, and 7.0-rc5.*
*   **CVE-2026-31408: Bluetooth: SCO: Fix use-after-free in sco_recv_frame() due to missing sock_hold** Concurrent close() can free the socket between the lock release and the subsequent sk->sk_state access, resulting in a use-after-free. *Fixed in versions 6.6.131, 6.12.80, 6.18.21, 6.19.11, and 7.0-rc6.*
*   **CVE-2026-31406: xfrm: Fix work re-schedule after cancel in xfrm_nat_keepalive_net_fini()** Can prevent xfrm net namespace freeing, resulting in use after free. *Fixed in versions 6.12.80, 6.18.21, 6.19.11, and 7.0-rc6.*
*   **CVE-2026-31407: netfilter: conntrack: add missing netlink policy validations** Out-of-bounds access in sctp and ctnetlink due to missing netlink policy validations. *Fixed in versions 6.19.10 and 7.0-rc5.*
*   **CVE-2026-31405: media: dvb-net: fix OOB access in ULE extension header tables** An out-of-bounds read occurs on the function pointer table, and the OOB value may be called as a function pointer. *Fixed in versions 6.1.167, 6.6.130, 6.12.78, 6.18.19, 6.19.9, and 7.0-rc3.*

## Application & Library Vulnerabilities

*   **Rembg: Path Traversal via Custom Model Loading** Allows unauthenticated remote attackers to read arbitrary files from the server's filesystem. *Restrict network access to the Rembg server. Refrain from providing arbitrary custom models.* GHSA-3wqj-33cg-xc48
*   **n8n-mcp: Unauthenticated Session Termination and Information Disclosure** HTTP transport endpoints lacked proper authentication, and the health check endpoint exposed sensitive operational metadata. *Fixed in v2.47.6. Restrict network access or use stdio mode.* GHSA-75hx-xj24-mqrw
*   **pypdf: Manipulated XMP Metadata Can Exhaust RAM** An attacker can craft a PDF which leads to large memory usage. *Fixed in pypdf==6.10.0.* GHSA-3crg-w4f6-42mx
*   **Arcane: Unauthenticated SSRF with Conditional Response Reflection** The /api/templates/fetch endpoint is vulnerable to unauthenticated SSRF. *Restrict network access.* GHSA-ff24-4prj-gpmj
*   **phpseclib: Variable-time HMAC comparison** Variable-time comparison in HMAC verification. Low severity (defense-in-depth) *Replace != with hash_equals().* GHSA-r854-jrxh-36qx
*   **basic-ftp: Incomplete CRLF Injection Protection Allows Arbitrary FTP Command Execution** Credentials and MKD commands are vulnerable to CRLF injection allowing arbitrary FTP command execution. *Update basic-ftp.* GHSA-6v7q-wjvx-w8wg
*   **Flux Notification-Controller: GCR Receiver Missing Email Validation** Allows unauthorized reconciliation triggering. *Upgrade to notification-controller v1.8.3.* GHSA-h9cx-xjg6-5v2w
*   **Step CA: Index Out of Bounds Panic in TPM Attestation EKU Validation** Vulnerable to an index out-of-bounds panic by sending a crafted attestation key (AK) certificate with an empty Extended Key Usage (EKU) extension during TPM device attestation.. *Upgrade to v0.30.0 or disable ACME provisioners using TPM device attestation.* GHSA-9qq8-cgcv-qmc9
*   **LangSmith Client SDKs: Prototype Pollution in langsmith-sdk** Contains an incomplete prototype pollution fix that can bypass the prototype pollution guard. *Update langsmith to 0.5.18.* GHSA-fw9q-39r9-c252
*   **goshs: File-Based ACL Authorization Bypass** State-changing routes bypass ACL/BasicAuth. An unauthenticated attacker can delete the `.goshs` file itself, the attacker can remove the folder's auth policy. *Update goshs.* GHSA-wvhv-qcqf-f3cx
*   **goshs: Missing Write Protection for Parametric Data Values** An unauthenticated attacker can delete the `.goshs` file itself, the attacker can remove the folder's auth policy. *Update goshs.* GHSA-2943-crp8-38xx
*    **nimiq-blockchain is missing a wall-clock upper bound on block timestamps** A malicious block-producing validator can set block timestamps arbitrarily far in the future, inflating the monetary supply beyond the intended emission schedule. *Update nimiq-blockchain.* GHSA-49xc-52mp-cc9j
*  **uv vulnerable to arbitrary file deletion through RECORD entries** An attacker must install *and* uninstall the malformed wheel to be affected.  Absolute paths in RECORD files are not allowed by the specification and, when present, uv always treats them as rooted in the wheel’s installation prefix. Absolute paths cannot be used to delete arbitrary files. *Versions 0.11.6 and newer of uv address the validation gap above, by [removing invalid entries from RECORD files on wheel installation](https://github.com/astral-sh/uv/pull/18943) and [ignoring RECORD paths that would escape the installation prefix on uninstall](https://github.com/astral-sh/uv/pull/18942).* GHSA-pjjw-68hj-v9mw
*   **SiYuan: Publish Reader Path Traversal Delete via `removeUnusedAttributeView`** A publish-service Reader context can delete arbitrary `.json` files reachable from the workspace.  *Update SiYuan.* GHSA-vw86-c94w-v3x4
*   **SiYuan: Publish Reader Can Arbitrarily Delete Attribute View Files via `/api/av/removeUnusedAttributeView`** An authenticated publish-service reader can invoke `/api/av/removeUnusedAttributeView` and cause persistent deletion of arbitrary attribute view definition files from the workspace..  *Update SiYuan.* GHSA-7m5h-w69j-qggg
*   **REDAXO has reflected XSS backend packages API via function parameter (CSRF token required)** User input `function` is injected into an exception message, then rendered by `rex_view::error()` which delegates to `rex_view::message()` without HTML escaping.  *Update REDAXO.* GHSA-xq4j-g85q-wf97
*   **REDAXO has reflected XSS in backend Metainfo API via type parameter (CSRF token required)** User input `type` is injected into an exception message, then rendered by `rex_view::error()` which delegates to `rex_view::message()` without HTML escaping..  *Update REDAXO.* GHSA-m662-8jrj-cw6v
*   **Ech0: Missing authorization on dashboard log endpoints allows low-privilege users to access sensitive system logs** Any valid user session can access historical system logs and can also connect to the SSE and WebSocket log streaming endpoints, increasing sensitive data available to attackers.  *Update Ech0 with authorization.* GHSA-cp79-9mwr-wr49
*  **Ech0: Scoped admin access tokens can bypass least-privilege controls on privileged endpoints, including backup export** A low scoped admin access token can reach broader admin functionality than intended, specifically data exfiltration.   *Update Ech0 with better authorization.* GHSA-4h9q-p5j4-xvvh
* **PraisonAI: SQLiteConversationStore didn't validate table_prefix when constructing SQL queries**

The `table_prefix` configuration value is directly used to construct SQL table identifiers without validation.

If an attacker controls this value, they can manipulate SQL query structure, leading to unauthorized data access (e.g., reading internal SQLite tables such as `sqlite_master`) and tampering with query results.  *Update PraisonAI.* GHSA-x783-xp3g-mqhp
* **PraisonAI Vulnerable to Server-Side Request Forgery via Unvalidated webhook_url in Jobs API** the server send POST requests to arbitrary internal or external destinations, enabling SSRF against cloud metadata services, internal APIs, and other network-adjacent services.   *Update PraisonAI with validated webhook_url.* GHSA-8frj-8q3m-xhgm
*   **PraisonAIAgents: Path Traversal via Unvalidated Glob Pattern in list_files Bypasses Workspace Boundary** allows the glob to traverse outside the validated directory..  *Update PraisonAI with validated Glob Pattern.* GHSA-7j2f-xc8p-fjmq
*   **PraisonAI Vulnerable to Sensitive Environment Variable Exposure via Untrusted MCP Subprocess Execution** any MCP command executed in this manner inherits all environment variables from the host process, including sensitive data such as API keys, authentication tokens, and database credentials..  *Update PraisonAI with restricted subprocess permissions.* GHSA-pj2r-f9mw-vrcq
* **PraisonAI: Unauthenticated Allow-List Manipulation Bypasses Agent Tool Approval Safety Controls**By adding dangerous tool names (e.g., `shell_exec`, `file_write`) to the allowlist, an attacker can cause the `ExecApprovalManager` to auto-approve all future agent invocations of those tools, bypassing the human-in-the-loop safety mechanism that the approval system is specifically designed to enforce. *Update PraisonAI with authenticated  allow-list.* GHSA-4wr3-f4p3-5wjh
*   **PraisonAIAgents has SSRF and Local File Read via Unvalidated URLs in web_crawl Tool** accept URLs from AI agents with zero validation..  *Update PraisonAI with validated URLs.* GHSA-8f4v-xfm9-3244
* **PraisonAI: Unauthenticated WebSocket Endpoint Proxies to Paid OpenAI Realtime API Without Rate Limits** The `/media-stream` WebSocket endpoint in PraisonAI's call module accepts connections from any client without authentication or Twilio signature validation. Each connection opens an authenticated session to OpenAI's Realtime API using the server's API key.  *Update PraisonAI with validated WebSockets.* GHSA-q5r4-47m9-5mc7
* **PraisonAIAgents: Hardcoded `approval_mode="auto"` in Chainlit UI Overrides Administrator Configuration, Enabling Unapproved Shell Command Execution** enables full system compromise in CI or shared deployment contexts. *Update PraisonAI with protected tool approval.* GHSA-ffp3-3562-8cv3
* **PraisonAI Vulnerable to Code Injection and Protection Mechanism Failure** AST-based Python sandbox can be bypassed.  *Update PraisonAI with code injection protection.* GHSA-3c4r-6p77-xwr7
* **PraisonAIAgents has an OS Command Injection via shell=True in Memory Hooks Executor (memory/hooks.py)**
Two independent attack surfaces exist. The first is via pre_run_command and post_run_command hook event types registered through the hooks configuration..  *Update PraisonAI to use shell=False.* GHSA-v7px-3835-7gjx
* **SiYuan Affected by Zero-Click NTLM Hash Theft and Blind SSRF via Mermaid Diagram Rendering** When a victim opens a note containing a malicious Mermaid diagram, the Electron client fetches the URL. On Windows, a protocol-relative URL (`//attacker.com/image.png`) resolves as a UNC path (`\\attacker.com\image.png`).  *Update SiYuan.* GHSA-w95v-4h65-j455
* **LXD: Importing a crafted backup leads to project restriction bypass** The backup import path has two distinct configuration sources within a single tar archive, which when crafted the proper way will bypass project restrictions.  *Update LXD.* GHSA-q96j-3fmm-7fv4
* **LXD: Update of type field in restricted TLS certificate allows privilege escalation to cluster admin** A restricted TLS certificate user can escalate to cluster admin.  *Update LXD.* GHSA-c3h3-89qf-jqm5
* **@sveltejs/kit: Unvalidated redirect in handle hook causes Denial-of-Service** causes an unhandled `TypeError` and could result in DoS on some platforms, especially if the location passed to `redirect` contains unsanitized user input. *Update @sveltejs/kit.* GHSA-3f6h-2hrp-w5wx
* **@sveltejs/adapter-node has a BODY_SIZE_LIMIT bypass** requests could bypass the `BODY_SIZE_LIMIT` on SvelteKit applications running with `adapter-node`. This bypass does not affect body size limits at other layers of the application stack, so limits enforced in the WAF, gateway, or at the platform level are unaffected. *Update @sveltejs/adapter-node.* GHSA-2crg-3p73-43xp
*   **Aiven Operator has cross-namespace secret exfiltration via ClickhouseUser connInfoSecretSource** A developer with create permission on ClickhouseUser CRDs in their own namespace can exfiltrate secrets from any other namespace with a single kubectl apply. The operator acts as a confused deputy: its ServiceAccount has cluster-wide secret read/write (aiven-operator-role ClusterRole), and it trusts user-supplied namespace values in spec.connInfoSecretSource.namespace without validation. No admission webhook enforces this boundary — the ServiceUser webhook returns nil, and no ClickhouseUser webhook exists.  *Update Aiven Operator to 0.37.0.* GHSA-99j8-wv67-4c72
* **Vikunja has File Size Limit Bypass via Vikunja Import**   An attacker can bypass the configured maximum file size limit. *Update Vikunja.* GHSA-qh78-rvg3-cv54
*   **Vikunja has HTML Injection via Task Titles in Overdue Email Notifications** injected Markdown constructs produce phishing links and tracking pixels in legitimate notification emails.  *Update Vikunja.* GHSA-45q4-x4r9-8fqj
* **Vikunja Missing Authorization on CalDAV Task Read**   Any authenticated CalDAV user who knows (or guesses) a task UID can read the full task data from any project on the instance. *Update Vikunja.* GHSA-48ch-p4gq-x46x
* **@vitejs/plugin-rsc has a Denial of Service with React Server Components** contains a vulnerability in versions prior to 19.2.4 *Update @vitejs/plugin-rsc@0.5.23 or later.* GHSA-v457-wxvj-p9w9
* **Vikunja has iCalendar Property Injection via CRLF in CalDAV Task Output**allows injection of arbitrary iCalendar properties such as `ATTACH`, `VALARM`, or `ORGANIZER`. *Update Vikunja.* GHSA-2g7h-7rqr-9p4r

## Notable Discussions

*   **GNU tar: listing/extraction desynchronization allows hidden file injection** - The original bug report and related replies are being discussed, CVE-2026-5704 has been assigned.  Concerns about people sending private messages on public mailing lists were raised in the thread.
*   **LibRaw 0.22.1 Release with security fixes** -  Includes fixes for vulnerabilities reported by Cisco Talos, including CVE-2026-20911 (heap-based buffer overflow).
*   **Roundcube arbitrary write + ID/XSS/etc.** - Multiple vulnerabilities fixed in Roundcube versions 1.5.14, 1.6.14, and 1.7 pre-releases.  Addendum: regressions from the mentioned versions were fixed March 29.
*   **CPython Base64 decoding & HTTP client proxy tunnel headers** - CVE-2026-3446 & CVE-2026-1502, CPython has several vulnerabilities.

I tried to make it as actionable as possible, focusing on the "what to do" aspect.
